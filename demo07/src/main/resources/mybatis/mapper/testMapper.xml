<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.demo07.mapper.TaUserMapper">
 <select id="login" resultType="TaUser">
  select * from ta_user where nickname=#{nickname} and password=#{password}
 </select>
<select id="getUserById" resultType="TaUser">
 select * from ta_user where user_id=#{userId}
</select>
 <select id="getTaUser" resultType="TaUser">
  select * from ta_user
 </select>
 <insert id="register" useGeneratedKeys="true" keyProperty="userId">
  insert into ta_user(nickname, avatar_url, level, gender, birthday, password,phone) VALUES (#{nickname},#{avatarUrl},#{level},#{gender},#{birthday},#{password},#{phone})
 </insert>
</mapper>
