<?xml version="1.0" encoding="utf-8" ?>

<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.demo07.mapper.TaCouponUserMapper">
    <insert id="insertTaCouponUser" useGeneratedKeys="true" keyProperty="taCouponUserId">
        insert into ta_coupon_user(coupon_id, user_id, order_id) VALUES (#{couponId},#{userId},#{orderId})
    </insert>
    <update id="changeTaCouponUser">
        update ta_coupon_user set order_id=#{orderId} where coupon_id=#{couponId} and user_id=#{userId}
    </update>
    <insert id="TaCouponInit">
        insert into ta_coupon_user(coupon_id, user_id) VALUES
        <foreach collection="list" separator="," item="item">
            (#{item.couponId},#{item.userId})
        </foreach>
    </insert>
    <delete id="deleteTaCoupon" >
        delete from ta_coupon_user where coupon_id=#{couponId} and coupon_user_id>0
    </delete>
</mapper>